# Enable STOMP adapter (TCP)
stomp.listeners.tcp.1 = 61613
stomp.default_user = guest

# Enable WebSocket over STOMP - CORRECTED
web_stomp.port = 15674

# Allow guest user from any origin
loopback_users = none