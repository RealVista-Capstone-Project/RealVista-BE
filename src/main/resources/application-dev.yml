spring:
  config:
    activate:
      on-profile: dev
  
  # Database Configuration
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:realvista_test}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  
  # Flyway - allow clean on validation error for dev (re-creates DB if migrations change)
  flyway:
    clean-disabled: false
    clean-on-validation-error: true

  # JPA Configuration
  jpa:
    show-sql: false
    properties:
      hibernate:
        format_sql: false
  
  # Cache
  cache:
    type: simple

# Logging
logging:
  level:
    root: INFO
    com.sep.realvista: DEBUG
    org.springframework.security: DEBUG

# Swagger
springdoc:
  swagger-ui:
    enabled: true

